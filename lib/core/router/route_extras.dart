/// Typed [GoRoute] extras (prefer over query strings for in-app navigation).
class AddExpenseRouteExtra {
  const AddExpenseRouteExtra({this.initialCategoryName});

  final String? initialCategoryName;
}
